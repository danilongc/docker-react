language: generic
sudo: required

services:
  - docker

before_install:
  - docker build -t danilongc/docker-react -f  Dockerfile.dev .

script:
  - docker run -e CI=true danilongc/docker-react npm run tets  -- --coverage